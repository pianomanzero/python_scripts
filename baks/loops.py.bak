#!/usr/bin/env python

#script to illustrate looping

the_count = [1,2,3,4,5]
fruits = ['apples','oranges','pears','apricots']
change = [1,'pennies',2,'dimes',3,'quarters']

#for loop that runs through a list
for number in the_count:
  print "This is count %d" % number

#same thing
for fruit in fruits:
  print "A fruit of type: %s" % fruit

#mixed lists, remember to use %r (for raw as opposed to %s for string and %d for decimal)
#since we dont' know what type of data is stored in this list
for i in change:
  print "I got %r" % i

#building lists
#start with an empty list...
elements = []

#then use range function to loop a specified number of times
for i in range(1,6):
  print "Adding %d to the list." % i
  #append is a list function usage to append to list
  elements.append(i)

#print out the elements list
for i in elements:
  print "Element #%d" % i

